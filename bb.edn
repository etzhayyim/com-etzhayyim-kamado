{:tasks
 {test {:doc "Run the complete standalone kamado suite"
        :task (shell "clojure -M -m kamado.test-runner")}
  audit {:doc "Validate canonical EDN and artifact boundaries"
         :task (shell "clojure -M scripts/audit.clj")}}}
